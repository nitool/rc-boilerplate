<!DOCTYPE HTML>
<html>
    <head>
        <title>{{ name }}</title>
    </head>
    <body>
        {{ insert_inline_styles(pharmacy, product) }}
        to jest przykład {{ name }}

        {{ insert_inline_scripts(pharmacy, product) }}
    </body>
</html>

